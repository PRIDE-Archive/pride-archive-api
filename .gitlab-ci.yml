image: maven:3.3.9-jdk-8

variables:
  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

before_script:
  - which ssh-agent || (apk add --update openssh-client bash openssl rsync gettext)
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
  - mkdir ~/.ssh
  - chmod 700 ~/.ssh
  - ssh-keyscan $DEPLOY_SERVER >> ~/.ssh/known_hosts
  - chmod 600 ~/.ssh/known_hosts

build:
  environment: '*'
  stage: build
  script:
    - mvn $MAVEN_CLI_OPTS compile

#test:
#  stage: test
#  script:
#    - mvn $MAVEN_CLI_OPTS test

deploy:
  stage: deploy
  environment: dev
  script:
    - mvn $MAVEN_CLI_OPTS package -DskipTests
    - envsubst < config/.application.yml > config/application.yml
    - cd target && jar_file=`ls *jar` && ln -s $jar_file $JAR_FILE_LINK && cd ..
    - ssh $SERVER_USER@$DEPLOY_SERVER "bash && cd $DEPLOY_PATH && BACKUP_DIR=backup_$(date +"%Y%m%d_%H%M%S") && mkdir $BACKUP_DIR && mv *jar $BACKUP_DIR/ && mv config $BACKUP_DIR/"
    - rsync -a target/*jar config $SERVER_USER@$DEPLOY_SERVER:$DEPLOY_PATH
    - ssh $SERVER_USER@$DEPLOY_SERVER "bash && cd $DEPLOY_PATH && sh $STOP_SCRIPT && sh $START_SCRIPT"
    #  only:
    ##    - master